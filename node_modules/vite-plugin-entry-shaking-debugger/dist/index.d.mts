import { ViteDevServer } from 'vite';
import { Context } from 'vite-plugin-entry-shaking';

/**
 * Attaches a debugger to a Vite dev server instance.
 * @param server Vite dev server instance.
 * @param ctx Plugin context.
 */
declare function attachDebugger(server: ViteDevServer, ctx: Context): Promise<void>;

export { attachDebugger, attachDebugger as default };
