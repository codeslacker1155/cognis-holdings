import { Text, View } from 'react-native';
import { YStack } from 'tamagui';

export default function Dashboard() {
    return (
        <YStack>
            <Text>Dashboard</Text>
        </YStack>
    );
}